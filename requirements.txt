# --- Core MVP (Phases 1 à 5) ---
pyspark==3.5.1
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.1        # IsolationForest
mlflow==2.14.1
fastapi==0.111.0
uvicorn==0.30.1
pydantic==2.8.2

# --- V2 (Phase 6 — LSTM Autoencoder) ---
# Décommenter au moment de la V2. Entraînement possiblement déporté sur Colab
# si la RAM 8 Go est insuffisante.
# tensorflow==2.16.1

# --- Dev ---
pytest==8.2.2
jupyter==1.0.0
